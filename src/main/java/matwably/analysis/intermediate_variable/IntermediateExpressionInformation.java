package matwably.analysis.intermediate_variable;

/**
 * This class serves to map Names in the program to Expressions. i.e. when
 * as we collect names in the alias_expression_map, we map them to an IntermediateExpression object
 * This object contains the expression and the statement it belongs to. We need the statement
 * as we need to know where the expression comes from.
 */
public class IntermediateExpressionInformation {
}

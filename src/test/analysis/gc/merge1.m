function merge1(a)
    b = ones(3,3)
    if a < rand()
        b = 3 % free b
        c = ones(2,2)
    else
        c = ones(2,2)
    end

    return;
    % Merge Op:
    % b dynamic
    % c static with count of 1.
end